# Runtime deps for the RAG plugin, pip-installed into Atlas's backend container
# by the plugin seam (plugin_seam.py) at startup. Intentionally separate from
# pyproject's [dependency-groups].dev, which uv manages for the local test env.
weaviate-client>=4.9,<5
httpx>=0.27
PyYAML>=6
